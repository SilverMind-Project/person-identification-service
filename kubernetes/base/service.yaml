apiVersion: v1
kind: Service
metadata:
  name: person-id-svc
  labels:
    app: person-id
spec:
  type: ClusterIP
  selector:
    app: person-id
  ports:
  - name: http
    port: 8100
    targetPort: 8100
    protocol: TCP
